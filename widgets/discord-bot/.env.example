# Copy to .env (same folder) — never commit .env
DISCORD_BOT_TOKEN=your_bot_token_here
# Optional: instant slash-command sync to one server (Developer Mode → Copy Server ID)
# DISCORD_GUILD_ID=123456789012345678