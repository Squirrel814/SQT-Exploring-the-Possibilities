# SQT Grove VS Code Extension (scaffold)

Press F5 in VS Code with this folder open (requires VS Code Extension development host).

Or package: `vsce package` (install `@vscode/vsce`).

Configuration: `sqtGrove.enginePath`, `sqtGrove.staticJsonPath`, `sqtGrove.insertFormat`.